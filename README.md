## Seed Users

This script creates three predefined test accounts for local authentication and RBAC testing.

### Prerequisites
- MongoDB is running
- The `.env` file is configured
- `MONGODB_URI` is set correctly

### Run the seed script
```bash
node seeds/user.seed.js